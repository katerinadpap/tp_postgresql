PostgreSQL Database Management System
=====================================

This directory contains the source code distribution of a temporal-probabilistic
(TP) extension of the PostgreSQL database management system, able to perform
queries including TP operations with negation (set-difference, left join, right
join), TP set operations (union, intersection), TP inner join and TP cartesian
product. 
